# Example environment variables for MarketPulse

# Flask environment
FLASK_ENV=production
FLASK_APP=src/app.py
PORT=10000

# Database (default is SQLite in /tmp)
DATABASE_URL=sqlite:////tmp/marketpulse.db
